--- src/MultiStepForm.tsx
+++ src/MultiStepForm.tsx
@@ -345,7 +345,15 @@
 
   const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {
     if (e.target.files && e.target.files[0]) {
-      setFormData(prev => ({ ...prev, buktiPembayaran: e.target.files![0] }));
+      const file = e.target.files[0];
+      // Check if file is greater than 2MB (2 * 1024 * 1024 bytes)
+      if (file.size > 2 * 1024 * 1024) {
+        alert('Ukuran file terlalu besar! Maksimal 2MB.');
+        e.target.value = ''; // Reset input
+        return;
+      }
+      setFormData(prev => ({ ...prev, buktiPembayaran: file }));
     }
   };
 
@@ -391,6 +399,13 @@
       const reader = new FileReader();
       reader.readAsDataURL(file);
       reader.onload = (event) => {
+        if (file.type === 'application/pdf') {
+          // Jika PDF, tidak bisa dikompres via canvas (Image).
+          // Langsung resolve dengan base64 raw string.
+          resolve(event.target?.result as string);
+          return;
+        }
+        
         const img = new Image();
         img.src = event.target?.result as string;
         img.onload = () => {
@@ -426,6 +441,12 @@
   const handleSubmit = async (e: React.FormEvent) => {
     e.preventDefault();
+
+    if (formData.metodePembayaran === 'Transfer' && !formData.buktiPembayaran) {
+      alert('Anda memilih metode pembayaran Transfer Bank. Mohon upload bukti transfer terlebih dahulu.');
+      return;
+    }
+
     setIsSubmitting(true);
     
     try {
@@ -1167,6 +1188,12 @@
                     </div>
                   </div>
                 )}
+
+                {formData.metodePembayaran === 'Tunai' && (
+                  <div className="animate-in fade-in slide-in-from-top-2 duration-300 mt-4">
+                    <div className="bg-orange-50 border border-orange-100 p-4 rounded-xl mb-4">
+                      <p className="text-sm text-orange-800 font-medium"><strong>Catatan:</strong> Pendaftar baru dapat mencetak lembar peserta tes setelah melakukan pembayaran tunai secara langsung di pondok pesantren.</p>
+                    </div>
+                  </div>
+                )}
 
               </div>
